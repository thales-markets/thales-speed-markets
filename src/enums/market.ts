export enum Positions {
    UP = 'UP',
    DOWN = 'DOWN',
}

export enum TradingSteps {
    ASSET = 1,
    DIRECTION = 2,
    TIME = 3,
    BUYIN = 4,
}

export enum SortDirection {
    NONE,
    ASC,
    DESC,
}

export enum HistoryStatus {
    OPEN = 'OPEN',
    CLAIMABLE = 'CLAIMABLE',
    WON = 'WON',
    LOSS = 'LOSS',
}
